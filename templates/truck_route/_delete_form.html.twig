<form method="post" action="{{ path('truck_route_delete', {'id': truck_route.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ truck_route.id) }}">
    <button class="btn">Delete</button>
</form>
