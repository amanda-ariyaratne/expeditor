<form method="post" action="{{ path('chain_manager_delete', {'id': chain_manager.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chain_manager.id) }}">
    <button class="btn">Delete</button>
</form>
