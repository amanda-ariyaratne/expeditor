<form method="post" action="{{ path('train_trip_delete', {'id': train_trip.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ train_trip.id) }}">
    <button class="btn">Delete</button>
</form>
