{% extends 'frontbase.html.twig' %}

{% block title %}Purchase{% endblock %}

{% block body %}
    <h1>Purchase</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ purchase.id }}</td>
            </tr>
            <tr>
                <th>Total_amount</th>
                <td>{{ purchase.totalAmount }}</td>
            </tr>
            <tr>
                <th>Delivery_date</th>
                <td>{{ purchase.deliveryDate ? purchase.deliveryDate|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Created_at</th>
                <td>{{ purchase.createdAt ? purchase.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>

        </tbody>
    </table>

    <a href="{{ path('purchase_index') }}">back to list</a>

    <a href="{{ path('purchase_edit', {'id': purchase.id}) }}">edit</a>

    {{ include('purchase/_delete_form.html.twig') }}
{% endblock %}
